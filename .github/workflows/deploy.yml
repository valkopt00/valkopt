name: Atualizar JSON de Notícias

on:
  schedule:
    - cron: "*/15 * * * *"  # Executa a cada 15 minutos
  workflow_dispatch:

jobs:
  update-json-data:
    name: Gerar e Atualizar Branch Data
    runs-on: ubuntu-latest

    steps:
      # ✅ OTIMIZAÇÃO 1: Checkout shallow inicial (apenas último commit)
      - name: Checkout otimizado do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 1  # Apenas o último commit (reduz de 45s para ~5-8s)
          token: ${{ secrets.GITHUB_TOKEN }}

      # ✅ OTIMIZAÇÃO 2: Fetch seletivo apenas das branches necessárias
      - name: Fetch branches necessárias
        run: |
          # Fetch apenas a branch data (se existir) e main
          git fetch origin data:data || echo "Branch data não existe ainda"
          git fetch origin main:main
          
          # Mudar para branch data
          if git show-ref --verify --quiet refs/heads/data; then
            git checkout data
          else
            git checkout --orphan data
            git reset --hard
          fi
          mkdir -p articles public
          
          # Criar ficheiro original_categories.json vazio se não existir
          if [ ! -f "articles/original_categories.json" ]; then
            echo '{"categories": {}, "articles": {}}' > articles/original_categories.json
          fi

      # ✅ OTIMIZAÇÃO 3: Cache para dependências Python
      - name: Configurar cache do pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # ✅ OTIMIZAÇÃO 4: Setup Python em paralelo com extração de scripts
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'  # Cache automático do pip

      # ✅ OTIMIZAÇÃO 5: Extração otimizada de scripts (usando refs locais)
      - name: Extrair scripts e requirements da branch main
        run: |
          mkdir -p temp-scripts/scripts/exporters
          
          # Usar referência local main em vez de origin/main (mais rápido)
          git show main:requirements.txt > temp-scripts/requirements.txt
          git show main:scripts/RSS_API.py > temp-scripts/scripts/RSS_API.py
          git show main:scripts/__init__.py > temp-scripts/scripts/__init__.py
          git show main:scripts/mappings.py > temp-scripts/scripts/mappings.py
          git show main:scripts/remove_articles.py > temp-scripts/scripts/remove_articles.py
          git show main:scripts/reset_counts_with_backup.py > temp-scripts/scripts/reset_counts_with_backup.py
          git show main:scripts/remove_duplicates.py > temp-scripts/scripts/remove_duplicates.py
          git show main:scripts/exporters/export_original_categories.py > temp-scripts/scripts/exporters/export_original_categories.py

      # ✅ OTIMIZAÇÃO 6: Instalação de dependências com cache
      - name: Instalar dependências
        run: |
          # Instalar com cache e sem upgrade desnecessário
          pip install --no-upgrade -r temp-scripts/requirements.txt

      - name: Executar script de atualização de JSON
        run: |
          export PYTHONPATH=$(pwd)/temp-scripts
          python temp-scripts/scripts/RSS_API.py

      # ✅ OTIMIZAÇÃO 7: Cópia otimizada de ficheiros HTML
      - name: Garantir pasta public e copiar ficheiros JSON
        run: |
          mkdir -p public
          # Copiar apenas os ficheiros JSON essenciais
          cp articles/articles.json public/
          cp articles/articles_search.json public/
          cp articles/original_categories.json public/
          
          # Cópia otimizada de ficheiros HTML estáticos (com verificação em batch)
          for file in "politica_de_privacidade.html" "termos_de_utilizacao.html" "contacto.html" "app-ads.txt"; do
            if git cat-file -e main:public/$file 2>/dev/null; then
              git show main:public/$file > public/$file
            fi
          done

      - name: Verificar ficheiros gerados
        run: |
          echo "📁 Ficheiros na pasta articles:"
          ls -la articles/
          echo ""
          echo "📁 Ficheiros na pasta public:"
          ls -la public/
          echo ""
          cd public
          if [ -f "articles.json" ]; then
            size=$(stat -c%s "articles.json")
            echo "   ✅ articles.json: $(numfmt --to=iec $size) (todas as notícias)"
          else
            echo "   ❌ articles.json NÃO foi criado"
          fi
          
          if [ -f "articles_search.json" ]; then
            size=$(stat -c%s "articles_search.json")
            echo "   ✅ articles_search.json: $(numfmt --to=iec $size) (dados de pesquisa)"
          else
            echo "   ❌ articles_search.json NÃO foi criado"
          fi
          
          if [ -f "original_categories.json" ]; then
            size=$(stat -c%s "original_categories.json")
            echo "   ✅ original_categories.json: $(numfmt --to=iec $size) (mapeamento)"
          else
            echo "   ❌ original_categories.json NÃO foi criado"
          fi

      - name: Criar README informativo
        run: |
          echo "# Branch Data - Sistema JSON Simplificado" > README.md
          echo "Atualizado automaticamente em: $(date '+%Y-%m-%d %H:%M:%S')" >> README.md
          echo "" >> README.md
          echo "## 🎯 Arquitetura simplificada:" >> README.md
          echo "Apenas **3 ficheiros JSON** essenciais, comprimidos automaticamente pelo Netlify (~65% redução):" >> README.md
          echo "" >> README.md
          echo "### 📱 Para a app principal:" >> README.md
          echo "- **articles.json** - Todas as categorias e artigos (ficheiro único)" >> README.md
          echo "" >> README.md
          echo "### 🔍 Para funcionalidade de pesquisa:" >> README.md  
          echo "- **articles_search.json** - Dados normalizados para pesquisa" >> README.md
          echo "" >> README.md
          echo "### 🗂️ Para mapeamento de categorias:" >> README.md
          echo "- **original_categories.json** - Mapeamento de categorias originais" >> README.md
          echo "" >> README.md
          echo "## ⚡ Performance esperada:" >> README.md
          cd public
          if [ -f "articles.json" ]; then
            size=$(stat -c%s "articles.json")
            compressed_size=$((size * 35 / 100))
            echo "- **Carregamento inicial**: ~$(numfmt --to=iec $compressed_size) (2-4 segundos)" >> ../README.md
            echo "- **Todas as categorias**: Disponíveis imediatamente após carregamento" >> ../README.md
            echo "- **Zero problemas de timing**: Sem dependências entre ficheiros" >> ../README.md
          fi

      # ✅ OTIMIZAÇÃO 8: Commit otimizado (verificação mais eficiente)
      - name: Commit e Push para branch data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          
          # Verificação otimizada de mudanças
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Atualização automática dos ficheiros JSON - $(date '+%Y-%m-%d %H:%M:%S')"
            git push origin data --force
          else
            echo "Nenhuma mudança para commitar"
          fi
